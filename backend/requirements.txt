# Versiones fijadas a las que corren en producción (VPS) para builds
# reproducibles entre despliegues (hallazgo medio del plan de mejora:
# "requirements.txt sin techos de versión" -> builds no reproducibles).
# Antes usaban ">=" abiertos, así que cada rebuild podía traer versiones
# distintas de dependencias de seguridad crítica (jose, passlib, pydantic).
# Para subir una dependencia, cámbiala aquí de forma consciente y prueba.
fastapi==0.141.1
uvicorn[standard]==0.52.4
sqlmodel==0.0.39
python-multipart==0.0.32
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
pydantic[email]==2.13.4
pydantic-settings==2.15.0
psycopg2-binary==2.9.12
alembic==1.19.1
httpx==0.28.1
